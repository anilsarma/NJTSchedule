<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_bookmarks"
        android:icon="@android:drawable/ic_menu_agenda"
        android:title="Settings"
        android:titleCondensed="Settings"
        android:visible="false"
        app:showAsAction="always"></item>
    <item

        android:id="@+id/menu_reverse"
        android:titleCondensed="Reverse"
        android:title="Reverse"
        android:visible="false"
        android:icon="@mipmap/ic_menu_swap_foreground"
        app:showAsAction="always">
    </item>
    <item
        android:id="@+id/menu_Refresh"
        android:titleCondensed="Refresh"
        android:title="Refresh"
        android:visible="false"
        android:icon="@android:drawable/ic_menu_rotate"
        app:showAsAction="always">
    </item>
    <item
        android:id="@+id/menu_Settings"
        android:titleCondensed="Settings"
        android:title="Settings"
        android:visible="false"
        android:icon="@android:drawable/ic_menu_manage"
        app:showAsAction="always">
    </item>
</menu>